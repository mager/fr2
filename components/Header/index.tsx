import * as React from "react";

import Container from "../Container";
import Nav from "../Nav";

const Header = () => {
  return (
    <header>
      <Container>
        <Nav />
      </Container>
    </header>
  );
};

export default Header;
